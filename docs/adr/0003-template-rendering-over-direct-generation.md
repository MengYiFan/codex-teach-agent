# ADR 0003：采用模板渲染而非模型直接生成最终文件

## 状态

Accepted

## 背景

模型直接生成 HTML、PPTX、PDF 或 DOCX 文件不可控，难以保证结构、样式、来源追溯、版本和安全。

## 决策

模型只输出结构化内容，最终 HTML/PDF/DOCX/PPTX 由模板渲染模块生成。

## 影响

- 提升导出稳定性和可测试性。
- 模板可作为商业化资产沉淀。
- 需要维护模板版本和内容 Schema。
